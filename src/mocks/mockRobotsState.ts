import RobotsState from "../redux/features/robots/types";
import { mockRobotsList } from "./mockRobots";

const mockRobotsState: RobotsState = {
  list: mockRobotsList,
};

export default mockRobotsState;
